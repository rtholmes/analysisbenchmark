TODO

DepFind:

- Class:				Record when a type extends multiple classes / interfaces (only one right now).
- Class: 				Explicitly record extends / implements relationships (not just outgoing type).
- Field / Parameter: 	Detect generic types; e.g., Vector<String> currently looks like Vector only; there should be a relationship for String too.
- Methods: 				Outbound types should include all parameter types and the return type.
- Methods: 				Explicitly differentiate between param and return types.
- Methods:				Explicitly capture param order.
- Methods:				Explicitly identify annotations as annotations.


 